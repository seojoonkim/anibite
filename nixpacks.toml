[phases.setup]
nixPkgs = ['nodejs_22', 'python311']

[phases.install]
cmds = [
  'pip install -r backend/requirements.txt',
  'cd frontend && npm ci'
]

[phases.build]
cmds = [
  'cd frontend && npm run build'
]

[start]
cmd = 'bash /app/docker-startup.sh'
