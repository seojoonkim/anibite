[build]
builder = "nixpacks"
nixpacksConfigPath = "nixpacks.toml"
# Only rebuild when backend files change
watchPaths = ["backend/**", "nixpacks.toml", "railway.toml"]

[build.nixpacks]
pythonVersion = "3.11"
